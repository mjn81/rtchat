import SignOut from '@/components/signOutButton';

import type { FC } from 'react';

interface ChatProps {
  
}

const Chat: FC<ChatProps> = async () => {
  return <pre>
    
  </pre>;
}

export default Chat;